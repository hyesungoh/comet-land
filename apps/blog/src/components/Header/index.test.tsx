import { MainHeader, PostHeader } from './index';

describe('blog - components - Header - index', () => {
  it('should defined MainHeader', () => {
    expect(MainHeader).toBeDefined();
  });

  it('should defined PostHeader', () => {
    expect(PostHeader).toBeDefined();
  });
});
